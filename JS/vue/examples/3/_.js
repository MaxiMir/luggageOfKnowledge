/**
  INIT:

  $ npm install -g @vue/cli

  $ vue ui # открывает страницы менеджера проектов Vue (Dashboard Vue)
  -> Создать 
      Выбрать: Users/Maxim/Documents/projects/
      * Создать новый проект здесь
  -> Подробнее:
      Папка проекта: vue-project
      Менеджер пакетов: npm
  -> Пресеты:
      Выберите пресет: Вручную
  -> Функции: 
      Babel, Progressive Web App (PWA) Support, Router, Vuex, CSS Pre-processors,
      Linter / Formatter, Использовать файл (Использовать отдельные файлы (такие как '.babelrc'))
  -> Конфигурация:
      + User history mode for router? (Requires proper setup for index fallback in production)
      Pick a CSS pre-processor: Sass/SCSS (with node-sass)


  Задачи -> serve 
      -> запустить задачу (<-> $ npm run serve)    
      -> октрыть приложение


  $ npm install materialize-css@next # устанавливаем Materialize CSS  
  $ npm install vuelidate --save # устанавливаем пакет для валидации
  $ npm i firebase # устанавливаем Firebase
  $ npm install vuejs-paginate --save # устанавливаем пагинацию
  $ npm i lodash
  $ npm install vue-chartjs chart.js # устанавливаем зависимости для графиков
  % npm install vue-meta --save # устанавливаем зависимость для мет
*/
